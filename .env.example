# AWS Configuration
# Set your AWS region (e.g., us-east-1, us-west-2, ap-southeast-2)
AWS_REGION=us-east-1

# AWS Bedrock Model Configuration
# You can use either a model ID or a full ARN
# Examples:
#   - Model ID: amazon.nova-lite-v1:0
#   - Full ARN: arn:aws:bedrock:REGION:ACCOUNT_ID:inference-profile/MODEL_ID
AWS_BEDROCK_MODEL_ID=amazon.nova-lite-v1:0

# AWS Credentials
# It's recommended to use AWS CLI configuration or IAM roles instead of hardcoding credentials
# If you must use credentials here, never commit this file to version control
# AWS_ACCESS_KEY_ID=your_access_key_here
# AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Optional: AWS Profile
# If you have multiple AWS profiles configured, specify which one to use
# AWS_PROFILE=default
